import type { EnvOptions } from 'tnp/src';

const env: Partial<EnvOptions> = {
  website: {
    domain: 'mouse-electron-jiggler.example.domain.com',
    title: 'Hello bb',
  },
};
export default env;
